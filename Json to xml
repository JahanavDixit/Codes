#include <json/json.h> #include <string>  std::string json_to_xml(const std::string& json_str) {     Json::Value root;     Json::Reader reader;     bool parsing_successful = reader.parse(json_str, root);     if (!parsing_successful) {         return "Error parsing JSON";     }     Json::StyledWriter writer;     return writer.write(root); }
